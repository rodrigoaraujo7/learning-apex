({
    apareceClick : function(component, event, helper) {
        helper.aparecerImg(component);
    },
    escondeClick : function(component, event, helper) {
        helper.esconderImg(component);
    }
})